import React from 'react';
import './Navigation.css';

const Navigation = () => (
  <nav className="navigation">
    <a href="#">Home</a>
    <a href="#">Reports</a>
  </nav>
);

export default Navigation;
